# 2026-06-13 · shutdown

## Shipped today
- CAIRN-007 cairn init adds .cairn/activity to root .gitignore
- CAIRN-008 Reword onboarding starter from delete to wontdo
- CAIRN-009 Namespace the CAIRN skills to avoid collisions (cairn- prefix)
- CAIRN-010 Two-line TUI header with logo and slogan
- CAIRN-011 Restructure the TUI into navigable tabs (Next/Inbox/Backlog/Help)
- CAIRN-012 Start backlog filter keys at 1, not 0
- CAIRN-013 Make the TUI fullscreen, fixed-height
- CAIRN-014 New braille logo + two-tone lit theme
- CAIRN-015 Refresh docs to match the current TUI
- CAIRN-016 Frontmatter parse failure destroys item fields (data-loss bug)
- CAIRN-017 Fix header row width to fill the box
- CAIRN-018 Make the ticket id link display and work
- CAIRN-019 Open and close items with Enter, not v
- CAIRN-020 Restore visible two-tone shading on the logo
- CAIRN-021 Make tab sections scroll, not overflow
- CAIRN-022 Batch cairn commits per session (new `batch` mode, default; flush at shutdown)
- CAIRN-023 Archive shipped/wontdo items to .cairn/archive/ so planning scans only live work
- (tooling) added the cairn-loop skill to work the backlog hands-free
- (maintenance) rewrote main history: collapsed 46 cairn: commits → 3 (one per shutdown),
  74 → 30 commits, tree byte-identical; backup tag backup/pre-history-rewrite

## In flight (each has a trusted next step)
- nothing — backlog fully cleared (22 shipped, 1 wontdo, inbox empty); board empty

## Captured (out of my head, safe until tomorrow)
- Force-push of the rewritten main to origin is the last step of today's session
  (origin/main still holds the old 74-commit history until then).
- Stale installed skills: ~/.claude/skills/cairn-* still describe the pre-`--flush`
  shutdown. Run `cairn install-skills --force` to sync the repo's updated copies
  (capture/plan/sync/shutdown now document batch + flush).
- Eyeball the TUI in a REAL terminal — smoke renderer can't pixel-verify colour/layout.
- Future `wontdo` items set outside advance() won't auto-archive; sweep with
  sweepTerminalItems if they accumulate (only 1 today, fine).

## Tomorrow — the one thing
- Verify the TUI/UX in a real terminal, then /cairn-capture whatever turns up.
  (No backlog work queued — the slate is clean.)
- Note: biweekly vote is due Monday 2026-06-15 → run /cairn-ballot then.

— Schedule shutdown complete.
