# Ballot · 2026-06-12  (vote closes Mon 2026-06-15)

Pick ONE to ship in the next two weeks. Both vetted against the code; both small + safe.

## Option A — Reword onboarding starter: delete → wontdo  (CAIRN-008)
- **benefit:** new users learn the real status-driven workflow, not an off-model `rm`
- **scope:** reword the seeded CAIRN-004 starter (title / summary / notes) and the
  "starter items — delete them once you are rolling" init message so both teach marking
  items `wontdo`
- **done when:** the seed + `cairn init` output reference `wontdo`, with no "delete"
- **out of scope:** the skills/workflow (already correct); reworking other starters
- **estimate:** ~0.5 day · confidence ~99%  (3 strings, all in `src/commands/init.ts`)

## Option B — Namespace the CAIRN skills with a `cairn-` prefix  (CAIRN-009)
- **benefit:** the five skills stop colliding with other skills in `~/.claude/skills`
- **scope:** rename `skills/{capture,plan,sync,shutdown,ballot}` → `cairn-*`; update each
  `name:` frontmatter, `src/core/skills.ts` `ORDER`, and all 32 `/command`
  cross-references (skill bodies, `init` output, TUI hints, README); reinstall and verify
  the legend shows `/cairn-capture` … `/cairn-ballot`
- **done when:** skills install + invoke as `/cairn-*`; legend + every reference match;
  build + render clean
- **out of scope:** the Claude Code plugin/package route (`cairn:capture`) — deferred to
  keep this a safe one-day rename
- **estimate:** ~1–1.5 days · confidence ~97%  (refs are all grep-findable)

---
_Note: thin backlog this cycle — these are the only two distinct feature/qol candidates
not already in flight. CAIRN-007 (root .gitignore) is excluded; it's the active action._
